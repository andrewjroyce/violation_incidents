Unzip files 
Right click inside the folder directory to open a terminal
Once inside the terminal type "yarn run", then hit return
The demo should run at localhost:3000 in the browser 
Brevety and clarity have been sacrificed due to project time limit